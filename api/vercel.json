{
  "version": 2,
  "buildCommand": "pnpm run vercel-build",
  "outputDirectory": "build",
  "builds": [
    {
      "src": "build/http/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/build/http/server.ts"
    }
  ]
}