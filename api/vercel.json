{
  "builds": [
    {
      "src": "api/http/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/http/server.ts" }
  ],
  "installCommand": "pnpm install",
  "buildCommand": "pnpm prisma migrate deploy"
}
